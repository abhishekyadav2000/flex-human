{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.23.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm exec turbo build --filter=@flex-human/web",
  "outputDirectory": "apps/web/.next"
}
